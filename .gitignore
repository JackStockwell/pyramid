# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# AI-agent skill files — regenerated by `npx convex ai-files install`,
# not hand-authored. (convex/_generated IS committed: `convex deploy --cmd`
# runs the frontend build *before* it regenerates that folder, so it has
# to already exist in the checkout for the build to find the API types.)
.agents/
.claude/skills/
skills-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
